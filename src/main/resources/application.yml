server:
  port : 8080
spring:
  thymeleaf:
    cache: false
    prefix: classpath:/templates/

  datasource:
    url: jdbc:mysql://localhost:3306/market?useSSL=false&serverTimezone=UTC&useUnicode=true&characterEncoding=UTF-8
    username: root
    password : acgjlm246!
    driver-class-name : com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect : org.hibernate.dialect.MySQL8Dialect
        ## show_sql: true
        format_sql: true

logging.level:
  org.hibernate.SQL: debug